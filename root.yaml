# Метаданные пакета
$package:
  seaf-core: # Идентификатор пакета
    # Краткое название пакета
    name: SEAF
    # Поставщик
    vendor: CEAF Team
    # Описание
    description: Community Enterprise Architect Framework
    # Версия
    version: 1.0.0
    # Зависимости от других пакетов
    dependencies:
      dochub: ">=4.1.0"

imports:
  - functions/root.yaml
  - entities/root.yaml
  - docs/root.yaml
