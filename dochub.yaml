# Метаданные пакета
$package:
  ceaf-core: # Идентификатор пакета
    # Краткое название пакета
    name: CEAF
    # Поставщик пакета
    vendor: CEAF Team
    # Описание  пакета
    description: Community Enterprise Architect Framework
    # Версия  пакета
    version: 1.0.0
    # Зависимости от других пакетов
    dependencies:
      app-ceaf-core: ">=1.0.0"
      # ba-ceaf-core: ">=1.0.0"
      # ia-ceaf-core: ">=1.0.0"
      # ta-ceaf-core: ">=1.0.0"

imports:  
  - _metamodel_/root.yaml